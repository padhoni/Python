// arr: array passed
for i in range(1, len(arr)):
    kVal=arr[i]
    j=i-1
    
    while j>=0 and arr[j] > kVal :
        arr[j+1] = arr[j]
        j=j-1
        
    arr[j+1]=kVal
        
for i in range (len(arr)):
    print(f"{arr[i]}")
